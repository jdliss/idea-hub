<!DOCTYPE html>
<html>
  <head>
    <title>IdeaHub</title>
     <!-- Compiled and minified CSS -->
     <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.6/css/materialize.min.css">
     <!-- Material Icons -->
     <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="navbar-fixed">
    <nav class="#3949ab indigo darken-1 z-depth-2">
      <div class="nav-wrapper">
        <div class="padded-left">
          <%= link_to "IdeaHub", ideas_path, { class: "brand-logo"} %>
        </div>
        <ul id="nav-mobile" class="right hide-on-med-and-down">
          <% if current_user %>
            <%= link_to "I HAVE AN IDEA!", new_user_idea_path(current_user) %>
            <%= link_to "Categories", categories_path %>
            <% if current_user.admin? %>
              <%= link_to "Create Category", new_admin_category_path %>
            <% end %>
            <%= link_to "Log Out (#{current_user.username})", logout_path, method: "delete" %>
          <% else %>
            <%= link_to "Register New User", new_user_path  %>
            <%= link_to "Log In", login_path %>
          <% end %>
        </ul>
      </div>
    </nav>
  </div>
    <div class="footer center">
      <% flash.each do |type, message| %>
          <script>
            $(function() {
              Materialize.toast('<%= message %>', 2500);
            });
          </script>
      <% end %>
      <%= yield %>
    </div>
    <footer class="#3949ab indigo darken-1 page-footer footer-small">
      <div class="container">
        <div class="row">
          <div class="col l6 s12">
            <h5 class="white-text">IdeaHub</h5>
            <p class="grey-text text-lighten-4">IdeaHub is a friendly place to share ideas about anything.  Enjoy using IdeaHub to get and give Ideas!  Images are automatically generated to support your ideas when you make them. Have fun!</p>
          </div>
          <div class="col l4 offset-l2 s12">
            <a class='dropdown-button btn-floating btn-large btn waves-effect default' href='#' data-activates='dropdown2'><i class="material-icons">add</i></a>
              <ul id='dropdown2' class='dropdown-content'>
                <li><%= link_to "Github Repo  ", "https://github.com/jdliss/idea-hub" %></li>
                <li><%= link_to "Rails", "http://rubyonrails.org/" %></li>
                <li><%= link_to "Materialize", "http://materializecss.com" %></li>
              </ul>
          </div>
        </div>
      </div>
    </footer>
    <!-- Compiled and minified JavaScript -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.6/js/materialize.min.js"></script>
  </body>
</html>
